# auto-semicolon package

This package will automatically move a semicolon to the outside of the last
bracket.

## Install

Using `apm`:

```shell
apm install auto-semicolon
```

Or Settings/Preferences ➔ Packages ➔ Search for `auto-semicolon`.
